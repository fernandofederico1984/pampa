{
  "specification" : "${specificationName}",
  "scenarios" : [
        <% testScenarios.each { %>
                { "name" : "${it.name}()",
                  "blocks": [ {
                      <% it.blocks.each { %>
                          "name" : "${it.name}",
                          "description" : "${it.description}" }
                      <% } %> ]
                }
          <%}%>
          ]
}