package ${destinationPackage}

import spock.lang.Shared

/**
 * ${specificationDescription}
 */
class ${specificationName} extends spock.lang.Specification {

    def setupSpec() {

    }

    def setup() {

    }

  <% testScenarios.each { %>
        def ${it.name}() {
          <% it.blocks.each { %>
              ${it.name}: ${it.description}
          <% } %>
        }
  <%}%>

    def cleanupSpec() {

    }
}
